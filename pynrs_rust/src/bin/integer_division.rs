fn main() {
    assert!( 2 / 3 == 0 &&  2 % 3 ==  2);
    assert!( 1 / 3 == 0 &&  1 % 3 ==  1);
    assert!( 0 / 3 == 0 &&  0 % 3 ==  0);
    assert!(-1 / 3 == 0 && -1 % 3 == -1);
    assert!(-2 / 3 == 0 && -2 % 3 == -2);
}
